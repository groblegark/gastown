apiVersion: v2
name: gastown
description: Gas Town deployment — wraps bd-daemon with environment-specific configuration
type: application
version: 0.1.0
appVersion: "0.57.11"

dependencies:
  - name: bd-daemon
    version: "~0.2.0"
    repository: oci://ghcr.io/groblegark/charts
